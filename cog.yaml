build:
  python_version: "3.10"
  system_packages:
    - ffmpeg
  python_packages:
    # --- Core PyTorch frameworks ---
    - torch==2.3.0
    - torchaudio==2.3.0
    - torchvision==0.18.0

    # --- DSP & Audio processing ---
    - librosa==0.11.0
    - soundfile==0.13.1
    - scipy==1.13.1
    - numpy==1.23.5
    - resampy==0.2.2

    # --- Config & utilities ---
    - omegaconf==2.3.0
    - tqdm==4.67.1
    - easydict==1.13

    # --- Pitch extraction (CREPE original, TensorFlow backend) ---
    - tensorflow==2.8.4
    - crepe==0.0.12
predict: "predict.py:Predictor"
gpu: true
